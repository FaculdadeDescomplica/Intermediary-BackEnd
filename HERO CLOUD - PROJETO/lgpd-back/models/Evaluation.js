class Evaluation {
    constructor(evaluationId, evaluationUserId, evaluationCourseId, evaluationConcept) {
        this.evaluation_id = evaluationId;
        this.evaluation_user_id = evaluationUserId;
        this.evaluation_course_id = evaluationCourseId;
        this.evaluation_concept = evaluationConcept;
    };
}

export default Evaluation;